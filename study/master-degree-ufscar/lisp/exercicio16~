;;;; Versão Recursiva
(defun cartesianoR (L1 L2)
  (cond ((null L1) nil)
        (T (append (combina (car L1) L2) 
                   (cartesiano (cdr L1) L2)))
  )
)

;; Função auxiliar que combina elemento 'e' com elementos de L
(defun combina (e L)
  (cond ((null L) nil)
        (T (append )
